---
'@flaghoist/mcp': minor
---

New package: MCP server for Flaghoist feature-flag management. Lets coding agents (Claude Code, Cursor, Windsurf) read and manage flags over the Model Context Protocol.

Read-only by default. Set `FLAGHOIST_MCP_ALLOW_WRITES=1` to enable write tools.
