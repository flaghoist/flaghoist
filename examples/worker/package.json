{
  "name": "@flaghoist/example-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Deploy a Flaghoist server as a Cloudflare Worker backed by Workers KV.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@flaghoist/adapter-cloudflare-kv": "workspace:*",
    "@flaghoist/server": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.119.0"
  }
}
